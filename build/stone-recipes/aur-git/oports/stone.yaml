name: oports
version: 0.1
release: 1
homepage: https://github.com/sdushantha/oports
upstreams: []
summary: A wrapper around ss -tunlp to display cleaner output
description: |
  A wrapper around 'ss -tunlp' to display cleaner output
license:
  - MIT
builddeps:
  - binary(install)
networking: true
setup: |
  git clone --depth=1 https://github.com/sdushantha/oports.git .
build: |
  echo 'Build not needed - shell script'
install: |
  %install_bin oports
