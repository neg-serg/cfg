name: mediainfolib
version: "26.05"
release: 1
homepage: https://github.com/MediaArea/MediaInfoLib
upstreams:
  - https://github.com/MediaArea/MediaInfoLib/archive/refs/tags/v26.05.tar.gz : f3fa9faa5623a5bacf6bed30c1d4c7434852be1654e655d458f964f597d98b8e
summary: MediaInfo library
description: |
  MediaInfoLib
license:
  - BSD-2-Clause
builddeps:
  - binary(cmake)
  - binary(gcc)
  - binary(g++)
  - binary(ninja)
  - pkgconfig(zlib)
  - zenlib-devel
setup: |
  %cmake -S Project/CMake \
    -DBUILD_ZENLIB=OFF \
    -DBUILD_ZLIB=OFF \
    -DBUILD_SHARED_LIBS=ON
build: |
  %cmake_build
install: |
  %cmake_install
