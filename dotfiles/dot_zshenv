# No-op guards for zi (not installed)
zi() { :; }
zsh-defer() { shift; "$@" 2>/dev/null || :; }
smartcache() { eval "$("$@")"; }

export ZDOTDIR="${XDG_CONFIG_HOME:-$HOME/.config}/zsh"

# Guix profile removed
