# VPN split router: probe timeouts, TTLs, seed failure thresholds.
# Consumed by network.sls → renders vpn-split-router.yaml.j2.
settings:
  probe_timeout_seconds: 3.0
  probe_interval_seconds: 900
  seed_vpn_failure_threshold: 1
  observed_vpn_failure_threshold: 2
  direct_ttl_seconds: 21600
  vpn_ttl_seconds: 21600
  observed_stale_after_seconds: 604800

seed_domains:
  - claude.ai
  - chatgpt.com
  - openrouter.ai
