name: test-simple
version: "1.0"
release: 1
homepage: https://example.com
summary: test
description: test
license: MIT
builddeps:
  - binary(make)
setup: |
  echo hello > test.txt
build: |
  true
install: |
  mkdir -p %(installroot)/usr/share
  cp test.txt %(installroot)/usr/share/
