name        : tree-sitter-cli
version     : 0.26.9
release     : 1
homepage    : https://crates.io/crates/tree-sitter-cli
summary     : Tree-sitter CLI tool
description : |
    CLI tool for the tree-sitter parsing library
license     : MIT
builddeps   :
    - binary(cargo)
networking  : true
build       : |
    cargo install tree-sitter-cli --version 0.26.9 --root %(installroot)%(prefix)
